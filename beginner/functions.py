#return statement = Functions send python values/objects back to the caller.
#                   these values/objects are known as the function's return value

#def multiply(number1,number2):
#    return number1 * number2
#print(multiply(5,6))

#keyword arguments = arguments preceded by an identifier when we pass them to a function
 #                   the order of the arguments doesn't matter, unlike positional arguments
#                   python knows the names of the arguments that our function recieves

#def hello(first,middle,last):
 #   print("Hello "+first+" "+middle+" "+last)
#hello("Swapna", "Nagendrarao","Suryan")
#hello(last="suryan",middle="Nagendrarao",first="Swapna")

#nested functions call = function calls inside other function calls
#                           innermost function calls are resolved first
#                           returned value is used as argument for the next outer function

#num = input("Enter a whole positive number: ")
#num = float(num)
#num = abs(num)
#num = round(num)
#print(num)

#num = round(abs(float(input("Enter a whole positive number: "))))
#print(num)



